// Base URL of the PersonaOn AI backend (Render in prod, localhost in dev).
// Empty string means "same origin" (not used now that AI lives in the backend).
export const API_BASE = process.env.NEXT_PUBLIC_API_BASE ?? "";
